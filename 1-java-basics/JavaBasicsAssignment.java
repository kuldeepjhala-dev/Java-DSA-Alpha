import java.util.Scanner;

public class JavaBasicsAssignment {
    public static void main(String[] args) {
        // 1. In a program, input 3 numbers : A, B and C. You have to output the average
        // of these 3 numbers.
        // Scanner sc = new Scanner(System.in);
        // int a = sc.nextInt();
        // int b = sc.nextInt();
        // int c = sc.nextInt();
        // int avg = (a + b + c) / 3;
        // System.out.println(avg);

        // 2. In a program, input the side of a square. You have to output the area of
        // the square.
        // Scanner sc = new Scanner(System.in);
        // double side = sc.nextDouble();
        // double area = side * side;
        // System.out.println(area);

        // 3. Enter cost of 3 items from the user (using float data type) - a pencil, a
        // pen and an eraser. You have to output the total cost of the items back to the
        // user as their bill.
        // (Add on : You can also try adding 18% gst tax to the items in the bill as an
        // advanced problem)
        // Scanner sc = new Scanner(System.in);
        // float pencil = sc.nextFloat();
        // float pen = sc.nextFloat();
        // float eraser = sc.nextFloat();
        // float totalCost = pencil + pen + eraser;
        // float withGstCost = (float) (totalCost * 0.18);
        // System.out.println("Total cost = " + totalCost);
        // System.out.println("Total cost with GST= " + withGstCost);

        int $ = 24;
        System.out.println($); // op: 14
    }
}
